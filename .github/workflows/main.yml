jobs:
  process-markdown:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2  
    - name: Set up Python
      uses: actions/setup-python@v2
      with:
        python-version: '3.x'
    - name: Run Script
      run: python convert_latex.py
      working-directory: . 
